{
    "name": "db",
    "type":"module",
    "main": "index.js",
    "scripts": {
        "docker-run": "docker run --detach --publish 27017:27017 --name cc-db mongo:latest",
        "seed": "set DBPORT=27017 && set DBHOST=localhost && node index.js"
    }
}